import {rosterGroups,normalizeRoster,officialData} from '@/lib/roster-model';
export async function GET(request:Request){const group=rosterGroups.find(g=>g.id===new URL(request.url).searchParams.get('group'));if(!group)return Response.json({error:'올바른 포지션을 선택해 주세요.'},{status:400});try{const data=group.id==='coach'?await officialData('coachsteplist'):await officialData('playerlist',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({'gameSearch.position':group.code})});return Response.json({people:normalizeRoster(data,group.id),checkedAt:new Date().toISOString()},{headers:{'Cache-Control':'no-store'}})}catch{return Response.json({error:'구단 선수단 정보를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요.'},{status:502,headers:{'Cache-Control':'no-store'}})}}
